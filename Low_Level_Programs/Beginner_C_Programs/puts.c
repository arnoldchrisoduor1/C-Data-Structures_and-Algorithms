#include <stdio.h>

int main(){
	puts("Hello World");
	puts("This should be a new line");
	puts("The puts fuction allows only for the printing of strings and not other data types while putting a new line at the end");
	return 0;
}
